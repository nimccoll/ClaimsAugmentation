{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/",
    "TenantId": "{your Azure AD tenant ID}",
    "ClientId": "{your Azure AD client ID for the authorization API}"
  },
  "Jwt": {
    "Issuer": "http://mysite.com", // This can be any value that represents your STS - must match the value used by the downstream API
    "Key": "njm456456&&^^%%&&^^%%567abc%%%" // This can be any string of ASCII characters - must match the value used by the downstream API
  },
  "AllowedHosts": "*"
}
